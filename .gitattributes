# Normalize to LF in the repo. The module ships shell scripts that run under
# Android's /system/bin/sh — a CRLF checkout on Windows would zip scripts with
# trailing \r and every line would fail at install time.
* text=auto eol=lf

*.sh    text eol=lf
*.prop  text eol=lf

*.png   binary
*.zip   binary
